CHIP Counter201807020 {
    IN inc,reset;
    OUT out[16];

    PARTS:
	Inc16(in=oo,out=incd);
	Mux16(a=oo,b=incd,out=o);
	Mux16(a=o,b=incd,out=uu);
	Mux16(a=uu,b[0..15]=false,out=this);
	Register(in=this,out=out,out=oo);

}